language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t sohinipattanayak/react-docker-travis-aws-beanstalk -f Dockerfile.dev .

script:
  - docker run -e CI=true sohinipattanayak/react-docker-travis-aws-beanstalk npm run test
